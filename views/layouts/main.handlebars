<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title class="main-title">Cruise Views</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Bungee+Spice&family=Russo+One&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.4/css/bulma.min.css">
    <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>



<body>
    <header>
        <h1 class="title center">CRUISE VIEWS</h1>
    </header>


    <div class="navbar-end">
        <div class="navbar-item">
            <div class="buttons">
                <a href='/home' class="button is-primary">
                    <strong>Home</strong>
                </a>
                {{#if loggedIn}}
                <a class="button is-light">
                    Log out
                </a>
                {{else}}
                <a href = '/login' class="button is-light">
                    Log in
                </a>
                {{/if}}
            </div>
        </div>
    </div>
    
    <main>
        <div class="gallery">
            {{{body}}}
        </div>
    </main>
    {{#if loggedIn}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>